```javascript
// ==UserScript==
// @name         Substack UI Orange to Black
// @namespace    http://tampermonkey.net/
// @version      1.0
// @description  Black Create button, black logo, white Subscribe button
// @author       Chatviro
// @match        *://*/*
// @grant        none
// ==/UserScript==

(function () {
    'use strict';

    const style = document.createElement('style');
    style.textContent = `

        /* =========================
           CREATE BUTTON
           ========================= */

        button[aria-label="Create"],
        button[aria-label="Create"]:hover,
        button[aria-label="Create"]:focus,
        button[aria-label="Create"]:active,
        .priority_primary-RfbeYt,
        .priority_primary-RfbeYt:hover,
        .priority_primary-RfbeYt:focus,
        .priority_primary-RfbeYt:active {
            background: #000 !important;
            color: #fff !important;
            border-color: #000 !important;
        }

        button[aria-label="Create"] svg,
        button[aria-label="Create"] svg *,
        .priority_primary-RfbeYt svg,
        .priority_primary-RfbeYt svg * {
            fill: #fff !important;
            color: #fff !important;
            stroke: #fff !important;
        }

        /* =========================
           SUBSCRIBE BUTTON
           ========================= */

        button.priority_tertiary-accent-_S602l,
        button.priority_tertiary-accent-_S602l:hover,
        button.priority_tertiary-accent-_S602l:focus,
        button.priority_tertiary-accent-_S602l:active {
            background: #fff !important;
            color: #000 !important;
            border-color: #fff !important;
        }

        button.priority_tertiary-accent-_S602l *,
        button.priority_tertiary-accent-_S602l:hover *,
        button.priority_tertiary-accent-_S602l:focus *,
        button.priority_tertiary-accent-_S602l:active * {
            color: #000 !important;
            fill: #000 !important;
            stroke: #000 !important;
        }

        /* =========================
           NAV LOGO SVG
           ========================= */

        svg[fill="var(--color-nav-logo)"],
        svg[fill="var(--color-nav-logo)"] *,
        svg[fill="var(--color-nav-logo)"] path,
        svg[fill="var(--color-nav-logo)"] g {
            fill: #000 !important;
            color: #000 !important;
            stroke: #000 !important;
        }

        /* Optional: change the CSS variable itself */
        :root {
            --color-nav-logo: #000 !important;
        }

    `;

    document.head.appendChild(style);
})();
```

This consolidates everything into a single userscript with one injected stylesheet.
